<form method="post" action="{{ path('audio_delete', {'id': audio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ audio.id) }}">
    <button class="btn">Delete</button>
</form>
