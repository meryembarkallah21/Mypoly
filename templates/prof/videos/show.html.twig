{% extends 'base.html.twig' %}

{% block title %}Videos{% endblock %}

{% block body %}
    <h1>Videos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ video.id }}</td>
            </tr>
            <tr>
                <th>Fichier</th>
                <td>{{ video.fichier }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('videos_index') }}">back to list</a>

    <a href="{{ path('videos_edit', {'id': video.id}) }}">edit</a>

    {{ include('prof/videos/_delete_form.html.twig') }}
{% endblock %}
