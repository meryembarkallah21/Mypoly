{% extends 'base.html.twig' %}

{% block title %}Ec{% endblock %}

{% block body %}
    <h1>Ec</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ec.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ ec.code }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ec.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ec_index') }}">back to list</a>

    <a href="{{ path('ec_edit', {'id': ec.id}) }}">edit</a>

    {{ include('ec/_delete_form.html.twig') }}
{% endblock %}
