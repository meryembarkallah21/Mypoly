{% extends 'base.html.twig' %}

{% block title %}Cycle{% endblock %}

{% block body %}
    <h1>Cycle</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cycle.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cycle.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cycle_index') }}">back to list</a>

    <a href="{{ path('cycle_edit', {'id': cycle.id}) }}">edit</a>

    {{ include('admin/cycle/_delete_form.html.twig') }}
{% endblock %}
